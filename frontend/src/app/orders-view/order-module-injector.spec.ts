import { OrderModuleInjector } from './order-module-injector';

describe('OrderModuleInjector', () => {
  it('should create an instance', () => {
    expect(new OrderModuleInjector()).toBeTruthy();
  });
});
